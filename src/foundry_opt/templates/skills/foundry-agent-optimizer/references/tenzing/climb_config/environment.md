# Environment

Everything an agent needs to set up the project and run the tooling.

## Toolchain

{{TOOLCHAIN}}

<!--
INIT fills this in: language(s), runtime/version, package manager, and any hardware notes
(e.g. "no GPU available", "offline — no network at run time").
-->

## Setup

{{SETUP_COMMANDS}}

<!--
INIT fills this in: the exact commands to install dependencies / build a working environment from a
fresh clone (e.g. `make venv && source .venv/bin/activate`, `npm ci`, `go mod download`).
-->

## Common commands

{{COMMON_COMMANDS}}

<!--
INIT fills this in: how to run tests, lint, build, etc. — the everyday commands an agent uses while
implementing an idea. If the project has none yet, write "None yet."
-->
