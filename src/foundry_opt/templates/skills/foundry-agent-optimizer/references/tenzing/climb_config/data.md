# Data

{{DATA_DETAILS}}

<!--
INIT includes this file only if the loop has data inputs and/or a held-out evaluation set.
Fill in:
  - Where the data lives (paths), its format, and how to load it.
  - Which parts are inputs the agent may use freely vs. a held-out set that must stay opaque.
  - Any confirmation the agent should get from the human before relying on a specific dataset.
If the loop has no data inputs at all, INIT deletes this file and the Data section of climb.md.
-->
